export type ProgressProps = {
	value: number;
    children?: React.ReactNode;
    size: number;
    text?: string
};

export type ProgressGeneral = {
    value: number;
    size: string;
    text: string
}